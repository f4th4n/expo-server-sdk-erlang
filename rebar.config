{erl_opts, [debug_info]}.

{deps, [
]}.

{shell, [
    {apps, [expo_server_sdk]}
]}.

{cover_enabled, true}.
{cover_opts, [verbose]}.

{profiles, [
    {prod, [{relx, [{dev_mode, false},
                    {include_erts, true}]}]
    },
    {test, [
        {deps, [
            {meck, "0.9.2"}
        ]}
    ]}
]}.

{project_plugins, [
    rebar3_hex
]}.

{hex, [
    {doc, #{provider => edoc}}
]}.